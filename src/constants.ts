export enum Port {
  HTTP_SERVICE = 3000,
  HTTP_COORDINATOR = 3001,
  WS_COORDINATOR = 4000,
  WS_HUB = 4001,
  WS_LIGHT = 4002,
}

export enum Colour {
  RED = "#ff0000",
  GREEN = "#00ff00",
  BLUE = "#0000ff",
  WHITE = "#ffffff",
}
